# x86 guest paravirt: kvm-clock/steal-time via KVM_GUEST, queued PV spinlocks,
# and the PVH entry point Cloud Hypervisor / Firecracker direct-boot uses.
CONFIG_KVM_GUEST=y
CONFIG_PARAVIRT_SPINLOCKS=y
CONFIG_PVH=y
# HYPERVISOR_GUEST is the x86 umbrella for guest paravirt.
CONFIG_HYPERVISOR_GUEST=y
