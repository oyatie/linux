# Per-version deltas for the 6.18 LTS (v1 plant) track.
# BOOTPARAM_SOFTLOCKUP_PANIC is a bool here; 7.x retyped it to int.
CONFIG_BOOTPARAM_SOFTLOCKUP_PANIC=y
# Randomized kmalloc caches (renamed to KMALLOC_PARTITION_CACHES in 7.2).
CONFIG_RANDOM_KMALLOC_CACHES=y
