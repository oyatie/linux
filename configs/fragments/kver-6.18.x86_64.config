# 6.18 x86 only: AES-GCM's GHASH is a standalone module here.  7.2 folded it
# into AES_NI_INTEL, so this symbol does not exist there (the two-track matrix
# caught exactly this drift -- it resolved on 6.18 and vanished on 7.2).
CONFIG_CRYPTO_GHASH_CLMUL_NI_INTEL=y
